<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo"
    android:versionCode="2"
    android:versionName="14 Parahs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="ubaidiyah.nafees.demo.activity.SurahListActivity"
            android:screenOrientation="sensorPortrait" />

        <activity android:name="ubaidiyah.nafees.demo.activity.ParahListActivity"
            android:screenOrientation="sensorPortrait" />

        <activity
            android:name="ubaidiyah.nafees.demo.activity.ReciteActivity"
            android:screenOrientation="sensorPortrait" />

        <activity
            android:name="ubaidiyah.nafees.demo.activity.MenuActivity"
            android:screenOrientation="sensorPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>